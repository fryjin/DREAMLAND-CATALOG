name: Apply offline submission fix

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/apply-offline-submit-fix.yml"
      - "scripts/apply-offline-submit-fix.mjs"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Apply offline submission protection
        run: node scripts/apply-offline-submit-fix.mjs

      - name: Validate offline submission protection
        run: node scripts/validate-offline-submit-fix.mjs

      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Strengthen offline submission protection"
            git push origin HEAD:main
          fi
